{{- if .Values.sequencing.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "aerie.fullname" . }}-sequencing
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "aerie.componentLabels" (dict "root" . "component" "sequencing") | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "aerie.componentSelectorLabels" (dict "root" . "component" "sequencing") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "aerie.componentSelectorLabels" (dict "root" . "component" "sequencing") | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z {{ include "aerie.postgresql.host" . }} {{ include "aerie.postgresql.port" . }}; do echo waiting for postgres; sleep 2; done']
        - name: wait-for-hasura
          image: busybox:1.36
          command: ['sh', '-c', 'until wget -q -O - http://{{ include "aerie.fullname" . }}-hasura:{{ .Values.hasura.service.port }}/healthz; do echo waiting for hasura; sleep 2; done']
      containers:
        - name: sequencing
          image: {{ include "aerie.image" (dict "root" . "repository" .Values.sequencing.image.repository "tag" .Values.sequencing.image.tag) | quote }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.sequencing.port }}
              protocol: TCP
          env:
            - name: AERIE_DB_HOST
              value: {{ include "aerie.postgresql.host" . | quote }}
            - name: AERIE_DB_PORT
              value: {{ include "aerie.postgresql.port" . | quote }}
            - name: SEQUENCING_DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "aerie.postgresql.secretName" . }}
                  key: sequencing-user
            - name: SEQUENCING_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "aerie.postgresql.secretName" . }}
                  key: sequencing-password
            - name: SEQUENCING_SERVER_PORT
              value: {{ .Values.sequencing.port | quote }}
            - name: SEQUENCING_LOCAL_STORE
              value: /usr/src/app/sequencing_file_store
            - name: HASURA_GRAPHQL_ADMIN_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "aerie.fullname" . }}-hasura-secrets
                  key: admin-secret
            - name: MERLIN_GRAPHQL_URL
              value: {{ include "aerie.hasura.graphqlUrl" . | quote }}
            - name: LOG_FILE
              value: console
            - name: LOG_LEVEL
              value: {{ .Values.sequencing.logLevel | quote }}
            - name: SEQUENCING_LANGUAGE
              value: {{ .Values.sequencing.language | quote }}
            - name: SEQUENCING_WORKER_NUM
              value: {{ .Values.sequencing.workerNum | quote }}
            - name: SEQUENCING_MAX_WORKER_NUM
              value: {{ .Values.sequencing.maxWorkerNum | quote }}
            - name: SEQUENCING_MAX_WORKER_HEAP_MB
              value: {{ .Values.sequencing.maxWorkerHeapMB | quote }}
            - name: TRANSPILER_ENABLED
              value: {{ .Values.sequencing.transpilerEnabled | quote }}
          volumeMounts:
            - name: file-store
              mountPath: /usr/src/app/sequencing_file_store
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            {{- toYaml .Values.sequencing.resources | nindent 12 }}
      volumes:
        - name: file-store
          persistentVolumeClaim:
            claimName: {{ include "aerie.fileStore.pvcName" . }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
